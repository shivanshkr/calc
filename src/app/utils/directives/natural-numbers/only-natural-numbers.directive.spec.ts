import { OnlyNaturalNumbersDirective } from './only-natural-numbers.directive';

describe('OnlyNaturalNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyNaturalNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
